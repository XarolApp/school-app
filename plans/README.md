# Improvement plans

Generated by `/improve` on 2026-08-24, against commit `12ede66`.

Scope of this run: the two mobile issues reported by the user — text not visible /
wrong centring on mobile, and bad scrolling behaviour on the paywall — plus one
related regression found while investigating. This was a **targeted** run, not a full
codebase audit (see "Not audited" below).

## Status

| # | Plan | Impact | Effort | Risk | Status |
|---|---|---|---|---|---|
| 001 | [Fix mobile preview harness clipping 28px](001-fix-mobile-preview-clipping.md) | HIGH | S | Very low | TODO |
| 002 | [Paywall sticky CTA block eats up to 57% of screen](002-fix-paywall-sticky-cluster.md) | HIGH | M | Medium | TODO |
| 003 | [Desktop breakpoint uses pre-redesign title sizes](003-fix-stale-breakpoint-title-sizes.md) | LOW-MED | S | Very low | TODO |
| 004 | [Implement the "School Search" design on `/skoly`](004-search-design-import.md) | HIGH | L | Medium | DONE |
| 005 | [Spacing & typography tokens: emit them, then adopt them site-wide](005-spacing-typography-migration.md) | HIGH | L | Medium | IN PROGRESS |
| 006 | [Comparison & decision tools (feature-brainstorm §5)](006-comparison-decision-tools.md) | HIGH | L | Medium | TODO |

> **004** was added 2026-08-30 by a separate `/improve plan <description>` run against
> commit `5a8381c` — a targeted single-plan run, not part of the 2026-08-24 audit above.
> It is independent of 001–003 (different files entirely). It ships **synthesized
> stand-in data** for fields the `schools` table lacks; that is a deliberate,
> user-approved pre-release tradeoff tracked in `UNFORGET.md` as a **hard launch blocker**.
>
> **006** was added 2026-09-10 by a `/plan-then-build` run against commit `1bf53fd`,
> not part of any `/improve` audit. It is a **feature build**, not a fix: every
> 🔥/✅ row of feature-brainstorm.md §5 except the full share-with-parent link
> (carved out into `UNFORGET.md` — it depends on the parent/child account and
> pricing model, which are unsettled). It is independent of 001–005 in code terms
> (four new pages, five new tables, one new script), but it **consumes** 005's
> spacing/type tokens, so if 005 changes a token name mid-flight, 006's
> `decision.css` follows it. Design canvas:
> https://claude.ai/code/artifact/688789aa-b54c-4a5e-b2b6-17b3ee775899
>
> **005** was added 2026-08-31, same variant, same base commit. It migrates the app onto
> `design/system`'s real spacing and type scales. **It interacts with 003**: 003 fixes a
> stale breakpoint title size in `onboarding.css`, which 005 explicitly excludes from
> scope — so 003 stays valid and independent, but per 005's `UNFORGET.md` entry it is
> better folded into the planned onboarding redesign than patched separately. 005 also
> supersedes the private `--space-*` block that 004 created inside `search.css`.

## Recommended order

**001 first, and separately from the others.** It is the actual cause of the reported
"text is not visible" symptom, it is a five-line change to a dev-only file, and until
it is fixed the preview harness misreports what every other change looks like. Fixing
it first means 002 can be verified through a preview that is telling the truth.

**002 second.** Independent of 001 in code terms (different files, no shared symbols),
but much easier to judge visually once 001 is done.

**003 any time.** Fully independent of both. Cosmetic and desktop-only.

No plan blocks any other at the code level; the ordering is about verification quality.

## Root causes, briefly

- **001** — `.phone` is 390px wide *including* 14px of bezel padding, so its inner
  window is 362px, but the iframe inside it is hard-coded to 390px with
  `overflow: hidden` on the container. 28px of the app's right edge and 28px of its
  bottom are clipped. **This bug is in the preview harness, not the onboarding.** The
  app itself measured clean at 390×844, 375×667, 360×560 and 320×480.
- **002** — the paywall's sticky bottom container holds four blocks (two buttons, a
  four-line trust list, a mock-payment note) totalling a fixed 317px. That is 38% of
  an iPhone 14 viewport and 57% of a 360×560 Android viewport. The
  `linear-gradient(… 78%, transparent)` on it lets content show through as it scrolls
  underneath, which is what read as a broken scroll animation. Every other screen's
  sticky bar is 11–19%.
- **003** — the `@media (min-width: 640px)` block was not updated during the
  2026-08-24 design-system port, so `.ob-title` shrinks from 32px to 30px as the
  viewport widens.

## Considered and rejected

- **"The onboarding layout is broken on mobile."** Measured and rejected. Zero
  horizontally-overflowing elements and zero top-clipped elements across four
  viewports down to 320×480. The reported symptom traces entirely to 001.
- **"The paywall has a broken scroll animation."** Rejected as stated — there is no
  scroll animation on the paywall. The real defect is sticky-element height (002); the
  gradient overlay makes it *look* like an animation artefact.
- **Adding a test framework** to make these verifiable automatically. Out of scope for
  a bug-fix run and a standing architectural decision the maintainer should make
  deliberately. All three plans use browser assertions instead.

## Not audited in this run

This was scoped to the two reported symptoms. Not examined: `server.js` (no
validation, no rate limiting, 4 flat routes — CLAUDE.md already flags this and
recommends `/improve` before serious backend work), Supabase/RLS posture, the matching
engine's correctness, dependency vulnerabilities, or the rest of the onboarding
screens beyond layout measurement. CLAUDE.md lists the milestones where a fuller audit
is worth running.
